import enum


class RawFigmaTokensDictKeys(enum.Enum):
    PRIMITIVES = "primitives"
    THEME = "theme"
    LIGHT_THEME = "light"
    COLORS = "Colors"
    COMPONENT_COLORS = "Component colors"
    SPACING = "Spacing"
    RADIUS = "radius"
    WIDTH = "widths"
    CONTAINER = "containers"
